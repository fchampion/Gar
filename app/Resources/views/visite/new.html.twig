{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création d'une visite</h1>
<ul>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        </br>
        <button class="btn waves-effect waves-light green" type="submit" name="Créer">Créer
            <i class="material-icons right">add</i>
        </button>
    {{ form_end(form) }}

    </br>
    <li>
        <a href="{{ path('visite_index') }}" class="waves-effect waves-light btn indigo darken-4">Revenir à la liste</a></td>
    </li>
</ul>
{% endblock %}
