{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modification de la promotion</h1>
<ul>
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
            <button class="btn waves-effect waves-light red orange" type="submit" name="Modifier">Modifier
            <i class="material-icons right">replay</i>
        </button>
    {{ form_end(edit_form) }}
    </br>
    <li>
        {{ form_start(delete_form) }}
            <button class="btn waves-effect waves-light red darken-2" type="submit" name="Supprimer">Supprimer
                <i class="material-icons right">delete</i>
            </button>
        {{ form_end(delete_form) }}
    </li>
    </br>
    <li>
        <a href="{{ path('promo_index') }}" class="waves-effect waves-light btn indigo darken-4">Revenir à la liste</a></td>
    </li>
</ul>
{% endblock %}
